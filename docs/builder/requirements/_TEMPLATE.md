---
stage: {阶段名}
status: draft            # draft → approved → implementing → qa-testing → human-testing → locked / blocked
created: {YYYY-MM-DD}
updated: {YYYY-MM-DD}
gate1_by:               # Gate 1 审批人（status=approved 时填写）
locked_by:              # 锁版人（status=locked 时填写）
deps: []
qa_issues: []
---

# 模块：{模块名称}

## 1. 功能概述

{用 2-3 句话说明这个模块做什么、为谁服务}

## 2. 前置依赖

| 依赖模块 | 依赖内容 | 接口/表 |
|---------|---------|--------|
| {依赖A} | {具体依赖什么能力} | {具体的函数签名或表名} |

> 注意：只引用 `docs/shared/contracts.md` 中已存在的接口。如果依赖模块尚未 locked，先标记为"待确认"。

## 3. 数据模型

### {表名}

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | bigint | PK AUTO | |
| ... | ... | ... | ... |

## 4. API 接口

### {METHOD} {/api/v1/path}

- **说明**：{接口用途}
- **认证**：{需要JWT / 不需要}
- **请求**：`{content-type}`
  - `{field}`: {说明}（{必填/选填}，{约束：长度/范围/格式}）
- **响应**：`{ code: 0, data: { ... } }`
- **错误码**：`401` {说明}，`404` {说明}

## 5. 业务规则

1. {规则1}
2. {规则2}
3. ...

## 6. 验收条件

- [ ] **{场景名称}**：Given {前置条件} → When {触发动作} → Then {期望结果}
- [ ] **{错误场景}**：Given {异常条件} → When {触发动作} → Then {期望错误码和提示}
- [ ] **{边界场景}**：Given {边界条件} → When {触发动作} → Then {期望结果}

> 要求：每条验收条件必须可验证（QA Agent 能直接据此构造测试输入）。
> 每个接口至少覆盖：Happy Path + 1 个 Error Case + 1 个 Edge Case。

## 7. 非功能约束

| 约束项 | 目标 | 测量方式 |
|--------|------|---------|
| 响应延迟 | {P99 ≤ Xms} | {接口名称} |
| 并发能力 | {并发量} | {场景描述} |
| 其他 | ... | ... |

## 8. 本期不做

- {明确列出本期不做的功能1}
- {明确列出本期不做的功能2}

> 注意：本期不做 ≠ 永远不做。这里列出的每一项都应该是一个明确的决策，防止实现Agent 过度工程或自由发挥。

## 9. 已知技术债

- {本阶段简化实现的点，后续阶段需要重构}
- {如："SSE 使用全局 map，阶段 3 重构为 Hub 模式"}

> 后续阶段读到此处时，知道这是计划内的重构点，不是意外发现的技术债。重构时仍需遵守 contracts.md 的接口保护规则。

# 接口契约

> **写入规则**：只有 `status: locked` 的阶段（人测试完全通过后）才能将其公开接口写入此文件。
>
> **读取规则**：后续阶段的需求 md 和主 Agent 的任务书应引用此文件中的接口签名。
>
> **保护规则**：`locked` 状态的接口签名不可变更，只能新增，不能修改或删除。

---

## 当前已锁版的阶段

<!-- 每完成一个阶段，在此追加 -->
<!-- 格式：
## 阶段N（locked，{YYYY-MM-DD}）
- `handler/func signature` → 返回值结构
- `pkg/func signature` → 返回值结构
- `METHOD /api/v1/path` → 请求/响应结构
-->

*暂无已锁版阶段*
